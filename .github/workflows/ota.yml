name: Build Web OTA Bundle

# Vérification CI uniquement : le bundle produit ici n'est PAS ce qui est servi en
# production. Sur Render, `npm run build` (build command du service) exécute
# scripts/build-ota.mjs directement, donc www/ota-manifest.json et www/ota-bundle.json
# sont régénérés à chaque déploiement à partir du code Web réellement déployé.
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-ota:
    name: Build Jarvis Command Center Web OTA Bundle
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci || npm install

      - name: Run Unit Tests
        run: npm test

      - name: Build TypeScript Code
        run: npm run build

      - name: Build OTA Bundle & Manifest
        run: node scripts/build-ota.mjs

      - name: Upload OTA Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-ota-bundle
          path: |
            www/ota-manifest.json
            www/ota-bundle.json
