diff --git a/node_modules/exceljs/lib/utils/iterate-stream.js b/node_modules/exceljs/lib/utils/iterate-stream.js
index 08b974e..01777dd 100644
--- a/node_modules/exceljs/lib/utils/iterate-stream.js
+++ b/node_modules/exceljs/lib/utils/iterate-stream.js
@@ -1,48 +1,48 @@
+// Rewritten (see patches/exceljs+4.4.0.patch) to remove the stream.pause()/resume() dance around
+// each yield. The original pause()-before-yield pattern is implicated in a documented Node >=18
+// race (https://github.com/exceljs/exceljs/issues/3064) where WorkbookReader intermittently fails
+// to observe every archive entry during a single streaming pass. This version never pauses the
+// source stream at all: it keeps a single permanent 'data' listener draining every chunk into a
+// queue, and simply waits for the next chunk (or end/error) when the queue runs dry. Nothing the
+// stream ever emits can be missed, because there is no window where events fire while we are not
+// actively listening for them.
 module.exports = async function* iterateStream(stream) {
   const contents = [];
-  stream.on('data', data => contents.push(data));
+  let ended = false;
+  let error = false;
+  let notify = null;

-  let resolveStreamEndedPromise;
-  const streamEndedPromise = new Promise(resolve => (resolveStreamEndedPromise = resolve));
+  const wake = () => {
+    if (notify) {
+      const resolve = notify;
+      notify = null;
+      resolve();
+    }
+  };

-  let ended = false;
+  stream.on('data', data => {
+    contents.push(data);
+    wake();
+  });
   stream.on('end', () => {
     ended = true;
-    resolveStreamEndedPromise();
+    wake();
   });
-
-  let error = false;
   stream.on('error', err => {
     error = err;
-    resolveStreamEndedPromise();
+    wake();
   });

-  while (!ended || contents.length > 0) {
-    if (contents.length === 0) {
-      stream.resume();
+  while (true) {
+    while (contents.length > 0) {
       // eslint-disable-next-line no-await-in-loop
-      await Promise.race([once(stream, 'data'), streamEndedPromise]);
-    } else {
-      stream.pause();
-      const data = contents.shift();
-      yield data;
+      yield contents.shift();
     }
     if (error) throw error;
+    if (ended) return;
+    // eslint-disable-next-line no-await-in-loop
+    await new Promise(resolve => {
+      notify = resolve;
+    });
   }
-  resolveStreamEndedPromise();
 };
-
-function once(eventEmitter, type) {
-  // TODO: Use require('events').once when node v10 is dropped
-  return new Promise(resolve => {
-    let fired = false;
-    const handler = () => {
-      if (!fired) {
-        fired = true;
-        eventEmitter.removeListener(type, handler);
-        resolve();
-      }
-    };
-    eventEmitter.addListener(type, handler);
-  });
-}
