import { WorkflowRegistry } from "../workflows/workflowRegistry.js";
import { randomUUID } from "node:crypto";
import { getDb } from "../persistence/db.js";
import type { ServiceOrchestrator } from "../orchestration/serviceOrchestrator.js";
import { savePlanCheckpoint } from "../persistence/checkpoint.js";
import { Planner, type ExecutionPlanNode, type PlanRun } from "./planner.js";
import type { ReplanningEngine } from "./replanningEngine.js";
import { SpecialistCoordinator,SpecialistRegistry } from "../orchestration/specialistRegistry.js";
import { MissionConsolidator } from "./missionConsolidator.js";
import { ActivityStore } from "../observability/activityStore.js";

const LIVE=new Set(["QUEUED","DISPATCHING","RUNNING"]);
export class PlanRunner {
 private timer?:NodeJS.Timeout; private ticking=false; readonly specialists:SpecialistRegistry; private coordinator:SpecialistCoordinator; private consolidator:MissionConsolidator; private activity=new ActivityStore();
 constructor(readonly orchestrator:ServiceOrchestrator,readonly planner=new Planner(),private replanning?:ReplanningEngine,private intervalMs=500,specialists?:SpecialistRegistry){this.specialists=specialists??new SpecialistRegistry(undefined,orchestrator.registry);this.coordinator=new SpecialistCoordinator(this.specialists);this.consolidator=new MissionConsolidator(orchestrator.artifacts);}
 start(){this.recover();this.timer=setInterval(()=>void this.tick(),this.intervalMs);this.timer.unref?.();}
 stop(){if(this.timer)clearInterval(this.timer);}
 recover(){getDb().prepare(`UPDATE plan_nodes SET claimed_at=NULL WHERE plan_run_id IS NOT NULL AND status='in_progress' AND operation_task_id IS NULL`).run();}
 async tick(){if(this.ticking)return;this.ticking=true;try{for(const run of this.planner.activeRuns()){try{await this.reconcile(run);}catch(error){this.planner.updateRun(run.id,"BLOCKED",error instanceof Error&&error.message==="INVALID_PERSISTED_PLAN_NODE"?"INVALID_PERSISTED_PLAN_NODE":"PLAN_RECONCILIATION_FAILED");}}}finally{this.ticking=false;}}
 private async reconcile(run:PlanRun){if(run.status==="PENDING"){this.planner.updateRun(run.id,"RUNNING");this.activity.append({dedupeKey:`plan-started:${run.id}`,planRunId:run.id,eventType:"PLAN_STARTED",message:"Mission started"});}let nodes=this.planner.nodes(run.id);const active=nodes.filter(n=>n.status==="in_progress"||n.status==="waiting");for(const node of active)await this.observe(run,node);const persistedRun=this.planner.getRun(run.id);if(persistedRun&&(persistedRun.status==="BLOCKED"||persistedRun.status==="FAILED"))return;nodes=this.planner.nodes(run.id);const stillActive=nodes.filter(n=>n.status==="in_progress");
  if(persistedRun?.pendingReplanNodeId){if(stillActive.length)return;const failed=nodes.find(n=>n.id===persistedRun.pendingReplanNodeId);if(failed){await this.replan(run,failed,failed.error??"REPLANNABLE_FAILURE");}return;}
  const current=nodes.filter(n=>n.status!=="abandoned"&&n.status!=="cancelled");const pending=current.filter(n=>n.status==="pending");if(!pending.length){if(stillActive.length||current.some(n=>n.status==="waiting"))return;if(current.some(n=>n.status==="failed"&&n.generation===run.generation)){this.planner.updateRun(run.id,"FAILED");return;}try{this.planner.updateRun(run.id,"CONSOLIDATING");this.activity.append({dedupeKey:`consolidation-started:${run.id}`,planRunId:run.id,eventType:"CONSOLIDATION_STARTED",message:"Mission consolidation started"});this.consolidator.consolidate(run,nodes);for(const artifact of this.orchestrator.artifacts.listByWorkspace(run.workspaceId! ).filter(a=>a.planRunId===run.id&&a.metadata?.final===true))this.activity.append({dedupeKey:`artifact-created:${artifact.id}`,planRunId:run.id,eventType:"ARTIFACT_CREATED",message:`Artifact ${artifact.id} created`,metadata:{artifactId:artifact.id,name:artifact.name}});getDb().prepare("UPDATE plan_runs SET consolidated_at=? WHERE id=?").run(Date.now(),run.id);this.activity.append({dedupeKey:`consolidation-completed:${run.id}`,planRunId:run.id,eventType:"CONSOLIDATION_COMPLETED",message:"Mission consolidation completed"});this.planner.updateRun(run.id,"COMPLETED");new WorkflowRegistry().reconcileSuccesses();this.planner.setStatus(run.rootNodeId,"done");this.activity.append({dedupeKey:`plan-completed:${run.id}`,planRunId:run.id,eventType:"PLAN_COMPLETED",message:"Mission completed"});}catch(e){this.planner.updateRun(run.id,"BLOCKED",(e as Error).message==="CONSOLIDATION_ARTIFACT_CONFLICT"?"CONSOLIDATION_ARTIFACT_CONFLICT":"CONSOLIDATION_FAILED");}return;}
  const done=new Set(nodes.filter(n=>n.status==="done").map(n=>n.id));const ready=current.filter(n=>n.status==="pending"&&n.dependencies.every(d=>done.has(d))).sort((a,b)=>(a.position??0)-(b.position??0)||a.createdAt-b.createdAt||a.id.localeCompare(b.id));if(!ready.length){if(!stillActive.length)this.planner.updateRun(run.id,"BLOCKED","NO_EXECUTABLE_STEP");return;}
  if(stillActive.length){const activeOperations=stillActive.map(n=>n.operationTaskId?this.orchestrator.store.getOperation(n.operationTaskId):null);if(activeOperations.some(op=>!op||!op.parallelAllowed))return;}const slots=Math.max(0,run.maxParallelism-stillActive.length);if(!slots)return;const first=ready[0],firstService=this.orchestrator.registry.findServiceForCapability(first.capability!);if(!firstService){this.planner.updateRun(run.id,"BLOCKED","INVALID_PERSISTED_PLAN_NODE");return;}const parallel=firstService.parallelSafeCapabilities?.includes(first.capability!)===true;if(!parallel&&stillActive.length)return;const counts=new Map<string,number>();for(const n of stillActive)if(n.specialistId)counts.set(n.specialistId,(counts.get(n.specialistId)??0)+1);const batch:ExecutionPlanNode[]=[];for(const n of ready){this.activity.append({dedupeKey:`step-ready:${n.id}:${n.attempt}`,planRunId:run.id,planNodeId:n.id,eventType:"STEP_READY",message:`Step ${n.id} is ready`});if(batch.length>=slots)break;const service=this.orchestrator.registry.findServiceForCapability(n.capability!);if(!service)continue;if(!parallel||service.parallelSafeCapabilities?.includes(n.capability!)!==true){if(!batch.length&&!stillActive.length)batch.push(n);break;}const specialist=n.specialistId?this.coordinator.registry.get(n.specialistId):this.coordinator.select(n.capability!);if(specialist&&(counts.get(specialist.id)??0)>=specialist.maxConcurrency)continue;batch.push(n);if(specialist)counts.set(specialist.id,(counts.get(specialist.id)??0)+1);}
  for(const node of batch)await this.dispatch(run,node,batch.length>1||stillActive.length>0);
 }
 private async dispatch(run:PlanRun,node:ExecutionPlanNode,parallelAllowed=false){const db=getDb(),specialist=node.specialistId?this.coordinator.registry.get(node.specialistId):this.coordinator.select(node.capability!),key=node.operationIdempotencyKey??`plan:${run.id}:node:${node.id}:attempt:${node.attempt}`;const now=Date.now();const claimed=db.prepare(`UPDATE plan_nodes SET status='in_progress',claimed_at=?,operation_idempotency_key=?,specialist_id=?,updated_at=? WHERE id=? AND status='pending' AND operation_task_id IS NULL`).run(now,key,specialist?.id??null,now,node.id).changes;if(!claimed)return;if(specialist)this.activity.append({dedupeKey:`specialist-assigned:${node.id}:${node.attempt}`,planRunId:run.id,planNodeId:node.id,specialistId:specialist.id,eventType:"SPECIALIST_ASSIGNED",message:`Specialist ${specialist.id} assigned to step ${node.id}`});
  let op=this.orchestrator.store.getByIdempotencyKey(key);if(!op){const result=await this.orchestrator.dispatchCapability({action:"DISPATCH_CAPABILITY",capability:node.capability!,objective:node.objective!,context:node.context,constraints:node.constraints,priority:node.priority},{executionMode:"background",idempotencyKey:key,traceId:`plan-${run.id}`,workspaceId:run.workspaceId,specialistId:specialist?.id,planRunId:run.id,planNodeId:node.id,parallelAllowed});op=this.orchestrator.store.getOperation(result.taskId);}
  if(!op){this.failNode(run,node,"OPERATION_NOT_PERSISTED",false);return;}db.prepare(`UPDATE plan_nodes SET operation_task_id=?,updated_at=? WHERE id=?`).run(op.taskId,Date.now(),node.id);this.activity.append({dedupeKey:`step-dispatched:${node.id}:${node.attempt}`,traceId:op.traceId,planRunId:run.id,planNodeId:node.id,operationTaskId:op.taskId,specialistId:specialist?.id,eventType:"STEP_DISPATCHED",message:`Step ${node.id} dispatched to ${op.selectedService}`,metadata:{selectedService:op.selectedService,capability:node.capability}});if(op.status==="WAITING_PERMISSION")this.activity.append({dedupeKey:`approval-required:${node.id}:${op.taskId}`,traceId:op.traceId,planRunId:run.id,planNodeId:node.id,operationTaskId:op.taskId,specialistId:specialist?.id,eventType:"APPROVAL_REQUIRED",message:`Approval required for step ${node.id}`});this.planner.updateRun(run.id,op.status==="WAITING_PERMISSION"?"WAITING_PERMISSION":"RUNNING");await this.observe(run,{...node,status:"in_progress",operationTaskId:op.taskId,operationIdempotencyKey:key});
 }
 private async observe(run:PlanRun,node:ExecutionPlanNode){let op=node.operationTaskId?this.orchestrator.store.getOperation(node.operationTaskId):null;if(!op&&node.operationIdempotencyKey){op=this.orchestrator.store.getByIdempotencyKey(node.operationIdempotencyKey);if(op)getDb().prepare(`UPDATE plan_nodes SET operation_task_id=?,updated_at=? WHERE id=?`).run(op.taskId,Date.now(),node.id);}if(!op){this.planner.setStatus(node.id,"pending");return;}
  if(LIVE.has(op.status)){this.planner.updateRun(run.id,"RUNNING");return;}if(op.status==="WAITING_PERMISSION"){this.planner.setStatus(node.id,"waiting");this.planner.updateRun(run.id,"WAITING_PERMISSION");return;}if(op.status==="WAITING_INPUT"){this.planner.setStatus(node.id,"waiting");this.planner.updateRun(run.id,"BLOCKED","WAITING_INPUT");return;}if(op.status==="COMPLETED"){getDb().prepare(`UPDATE plan_nodes SET status='done',result=?,claimed_at=NULL,updated_at=? WHERE id=?`).run(op.result??null,Date.now(),node.id);this.activity.append({dedupeKey:`step-completed:${node.id}:${op.taskId}`,traceId:op.traceId,planRunId:run.id,planNodeId:node.id,operationTaskId:op.taskId,specialistId:node.specialistId,eventType:"STEP_COMPLETED",message:`Step ${node.id} completed`});this.planner.updateRun(run.id,"RUNNING");return;}if(op.status==="REJECTED"||op.status==="CANCELLED"){this.failNode(run,node,op.error??op.status,false,"BLOCKED");return;}
  if(op.status==="FAILED"){const unknown=(op.error??"").includes("TRANSPORT_UNKNOWN")||(op.error??"").includes("INTERRUPTED_EXECUTION_STATE_UNKNOWN");const event=this.orchestrator.store.listEvents(op.taskId).filter(e=>e.type==="TASK_FAILED").at(-1);const safe=!unknown&&event?.payload.replannable===true&&event.payload.side_effect_state==="none";if(!safe){this.failNode(run,node,op.error??"UNSAFE_FAILURE",false,unknown?"BLOCKED":"FAILED");return;}const db=getDb(),now=Date.now(),error=op.error??"REPLANNABLE_FAILURE";const outcome=db.transaction(()=>{db.prepare("UPDATE plan_nodes SET status='failed',error=?,claimed_at=NULL,updated_at=? WHERE id=?").run(error,now,node.id);const row=db.prepare("SELECT pending_replan_node_id pending FROM plan_runs WHERE id=?").get(run.id) as {pending:string|null};if(row.pending===null){db.prepare("UPDATE plan_runs SET pending_replan_node_id=?,updated_at=? WHERE id=?").run(node.id,now,run.id);return "claimed";}return row.pending===node.id?"same":"multiple";})();this.activity.append({dedupeKey:`step-failed:${node.id}:${op.taskId}`,traceId:op.traceId,planRunId:run.id,planNodeId:node.id,operationTaskId:op.taskId,specialistId:node.specialistId,eventType:"STEP_FAILED",level:"error",message:`Step ${node.id} failed`});if(outcome==="multiple"){this.planner.updateRun(run.id,"BLOCKED","MULTIPLE_PARALLEL_FAILURES");this.activity.append({dedupeKey:`plan-blocked:${run.id}:multiple-parallel-failures`,planRunId:run.id,eventType:"PLAN_BLOCKED",level:"error",message:"Mission blocked by multiple parallel failures"});return;}this.activity.append({dedupeKey:`replan-pending:${node.id}`,traceId:op.traceId,planRunId:run.id,planNodeId:node.id,operationTaskId:op.taskId,specialistId:node.specialistId,eventType:"REPLAN_PENDING",message:`Replan pending for step ${node.id}`});}
 }
 private failNode(run:PlanRun,node:ExecutionPlanNode,error:string,_replan:boolean,status:"FAILED"|"BLOCKED"="FAILED"){this.activity.append({dedupeKey:`step-failed:${node.id}:${node.operationTaskId??node.attempt}`,planRunId:run.id,planNodeId:node.id,operationTaskId:node.operationTaskId,specialistId:node.specialistId,eventType:"STEP_FAILED",level:"error",message:`Step ${node.id} failed`});if(status==="BLOCKED")this.activity.append({dedupeKey:`plan-blocked:${run.id}:${node.id}`,planRunId:run.id,planNodeId:node.id,eventType:"PLAN_BLOCKED",level:"error",message:`Mission blocked at step ${node.id}`});getDb().prepare(`UPDATE plan_nodes SET status='failed',error=?,claimed_at=NULL,updated_at=? WHERE id=?`).run(error,Date.now(),node.id);this.planner.updateRun(run.id,status,error);}
 private failReplan(run:PlanRun,node:ExecutionPlanNode,error:string){const now=Date.now();getDb().transaction(()=>{getDb().prepare("UPDATE plan_nodes SET status=\'failed\',error=?,claimed_at=NULL,updated_at=? WHERE id=?").run(error,now,node.id);getDb().prepare("UPDATE plan_runs SET status=\'FAILED\',last_error=?,pending_replan_node_id=NULL,updated_at=? WHERE id=?").run(error,now,run.id);})();}
 private async replan(run:PlanRun,node:ExecutionPlanNode,error:string){
  if(!this.replanning||run.replanCount>=run.maxReplans){this.failReplan(run,node,error);return;}
  const all=this.planner.nodes(run.id),affectedIds=new Set([node.id]);let changed=true;
  while(changed){changed=false;for(const candidate of all){if(candidate.status==="pending"&&!affectedIds.has(candidate.id)&&candidate.dependencies.some(dependency=>affectedIds.has(dependency))){affectedIds.add(candidate.id);changed=true;}}}
  const failed={...node,status:"failed" as const,error};
  const affected=[failed,...all.filter(candidate=>candidate.id!==node.id&&affectedIds.has(candidate.id))];
  const preservedPending=all.filter(candidate=>candidate.status==="pending"&&!affectedIds.has(candidate.id));
  const completed=all.filter(candidate=>candidate.status==="done");
  const externalDependencies=new Set<string>();
  for(const candidate of affected)for(const dependency of candidate.dependencies){const target=all.find(existing=>existing.id===dependency);if(!affectedIds.has(dependency)&&target&&target.status!=="done"&&target.status!=="abandoned"&&target.status!=="cancelled"&&target.status!=="failed")externalDependencies.add(dependency);}
  const capabilities=[...new Set(this.orchestrator.registry.listServices().filter(service=>service.enabled).flatMap(service=>service.capabilities))];
  savePlanCheckpoint(run.id,this.planner);this.activity.append({dedupeKey:`replan-started:${run.id}:${run.generation}`,planRunId:run.id,planNodeId:node.id,eventType:"REPLAN_STARTED",message:`Replan generation ${run.generation+1} started`});let specs;
  try{specs=await this.replanning.propose({objective:run.objective,completed,failed,affected,preservedPending,capabilities});}catch(e){this.failReplan(run,node,`REPLAN_FAILED: ${(e as Error).message}`);return;}
  const normalize=(value:string)=>value.normalize("NFKC").trim().replace(/\s+/g," ").toLocaleLowerCase();
  const preservedSignatures=new Set(preservedPending.map(candidate=>`${normalize(candidate.capability!)}\u0000${normalize(candidate.objective!)}`));
  if(specs.some(spec=>preservedSignatures.has(`${normalize(spec.capability)}\u0000${normalize(spec.objective)}`))){this.failReplan(run,node,"REPLAN_DUPLICATES_PRESERVED_STEP");return;}
  const obsolete=[...affectedIds].filter(id=>id!==node.id),db=getDb(),now=Date.now(),generation=run.generation+1;
  db.transaction(()=>{const abandon=db.prepare(`UPDATE plan_nodes SET status='abandoned',updated_at=? WHERE id=? AND status='pending'`);for(const id of obsolete)abandon.run(now,id);db.prepare(`UPDATE plan_nodes SET status='failed',error=?,claimed_at=NULL,updated_at=? WHERE id=? AND status!='done'`).run(error,now,node.id);const ids=new Map(specs.map(spec=>[spec.local_id,randomUUID()]));const q=db.prepare(`INSERT INTO plan_nodes(id,parent_id,title,status,created_at,plan_run_id,position,generation,capability,objective,context_json,constraints_json,priority,dependencies_json,updated_at,attempt)VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,1)`);specs.forEach((spec,index)=>{const dependencies:string[]=spec.depends_on.map(dependency=>ids.get(dependency)!);if(spec.depends_on.length===0)dependencies.push(...externalDependencies);q.run(ids.get(spec.local_id),run.rootNodeId,spec.title,"pending",now,run.id,index,generation,spec.capability,spec.objective,JSON.stringify(spec.context),JSON.stringify(spec.constraints),spec.priority,JSON.stringify([...new Set(dependencies)]),now);});db.prepare(`UPDATE plan_runs SET status='RUNNING',generation=?,replan_count=replan_count+1,last_error=?,pending_replan_node_id=NULL,updated_at=? WHERE id=?`).run(generation,error,now,run.id);})();this.activity.append({dedupeKey:`replan-applied:${run.id}:${generation}`,planRunId:run.id,eventType:"REPLAN_APPLIED",message:`Replan generation ${generation} applied`});
 }
 cancel(runId:string){this.activity.append({dedupeKey:`plan-cancel-requested:${runId}`,planRunId:runId,eventType:"PLAN_CANCEL_REQUESTED",message:"Mission cancellation requested"});const run=this.planner.getRun(runId);if(!run)return null;let uncertain=false;for(const n of this.planner.nodes(runId)){if(n.status==="pending")this.planner.setStatus(n.id,"cancelled");if(n.operationTaskId&&(n.status==="in_progress"||n.status==="waiting")){const r=this.orchestrator.store.cancel(n.operationTaskId);if(r&&!r.cancelled)uncertain=true;else this.planner.setStatus(n.id,"cancelled");}}this.planner.updateRun(runId,uncertain?"BLOCKED":"CANCELLED",uncertain?"CANCELLATION_REQUESTED_OPERATION_IN_PROGRESS":undefined);if(!uncertain){this.planner.setStatus(run.rootNodeId,"cancelled");this.activity.append({dedupeKey:`plan-cancelled:${runId}`,planRunId:runId,eventType:"PLAN_CANCELLED",message:"Mission cancelled"});}return this.planner.getRun(runId);}
}
